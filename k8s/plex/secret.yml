apiVersion: v1
kind: Secret
metadata:
  name: plex-credentials
  namespace: homelab
type: Opaque
stringData:
  # Replace with a fresh claim token from https://plex.tv/claim
  PLEX_CLAIM: "claim-REPLACE_ME"
